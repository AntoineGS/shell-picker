# Source parity matrix

The executable matrix has 371 intended assertions: codec: 50, batch-encoder: 2, directory-enumeration: 72, cd-merged: 8, operations: 34, slash: 28, modal: 20, create: 51, preview: 3, zshrc-cd: 42, zshrc-cp: 43, zshrc-add-mode-query-bindings: 6, and zshrc-add-mode-navigation-bindings: 12. It verifies 365 green legacy assertions; the remaining legacy suite fails at assertion four after three successful assertions.

The five authority sources and exact SHA-256 values are: `fzf-picker-candidates.zsh` `5300b66b7815e8b1c2f75f230033a069a1c305600faea164c9214cd52e07cb97`; `fzf-preview.sh` `232eb46eef32bff642985e42edbf0cd3a49098e7485eb6f5b0db0bdf48024159`; `fzf-batch-encode.pl` `055d9a74cce513bbf02475fae97154b159de98d51cc565cf4859ced0226878fd`; `fzf-picker.test.zsh` `f920b8f6194c76d5f8a1737c6e4860ab04f641da291dc3e984cbb63443552776`; and `.zshrc` `3bc868023693945a97b2e23f8f806ae5bdaa228a9898fb86cd8ad075e559ab18`.

Comparator fixtures cover batch encoder, codec records, cd/cp order, plus operations, slash, modal, create, preview, Zsh adapter, and Windows path categories. Native both-picker ignore behavior is intentionally outside byte-for-byte comparison. Run `go test ./integration -run 'TestEveryParityRowHasExecutableRunner|TestParityManifestCoverage' -count=1` for the differential matrix; category fixtures declare the six replacements: deterministic non-locale sorting, no legacy cache-basename collisions, useful missing-tool fallbacks, full-record authorization, transactional state replacement, and safe Add traversal/rollback.

Windows semantic substitutions replace drive root and UNC share root `virtual ..` with the exact `drives` token/canonical payload and authoritative Drives navigation. Non-root filesystem records are unchanged, Drives itself has no dot record, and virtual records are rejected for preview/final output. The default cached policy is a launch performance policy, not freshness parity: `--zoxide-policy fresh --zoxide-timeout 0` makes only the eligible initial launch query authoritative. Zoxide candidates can appear only in the initial CD view; every later generation, including a return to the launch path, uses local candidates only.
